# 云台测试日志

日期：2026-05-04  
仓库：`D:\ACE\Gv3\Gimbalv3`  
执行环境：`.venv\Scripts\python.exe`，Python 3.11.9  
相关规则：见 `测试规则.md`

## 本轮测试约束

1. 每次只跑一个实验命令。
2. 所有 RTT 实验都使用 Python 虚拟环境执行。
3. 每次实验前尽量回到中心稳定姿态。
4. 每次实验后必须回中收尾，再进入下一轮。
5. 实验理论位移必须远离 yaw/pitch 限位。
6. 调参永远不加 D，所有 `*_kd` 保持为 0。

## 连接情况

第一次普通权限运行 RTT 实验失败：

```text
error: Cannot connect to J-Link.
```

随后使用硬件访问权限后，J-Link 和 RTT 通路正常，能执行：

```text
tune get
tune set
tune sample on
tune speed_step
tune hold
```

## 当前参数基线

RTT `tune get` 读取到的主要参数：

```text
yaw_location_kp=18
yaw_location_ki=0
yaw_location_kd=0
yaw_speed_kp=30
yaw_speed_ki=5
yaw_speed_kd=0

pitch_location_kp=18
pitch_location_ki=4
pitch_location_kd=0
pitch_speed_kp=15
pitch_speed_ki=3
pitch_speed_kd=0

yaw_max_vel_dps=180
yaw_max_acc_dps2=720
yaw_k_vel_ff=50 -> 后续试降到 40
yaw_hold_ff=0

pitch_max_vel_dps=120
pitch_max_acc_dps2=480
pitch_k_vel_ff=0.08
pitch_hold_ff=0
```

## 已执行测试

### 1. yaw 速度阶跃，20 dps，`yaw_k_vel_ff=50`

输出目录：

```text
tools/out/tune/20260504_203256
```

命令要点：

```text
preset=speed-yaw
amp=20 dps
hold_ms=1200
duration=8 s
yaw_k_vel_ff=50
```

结果摘要：

```text
yaw 实测速度范围：-36 ~ +36 dps
yaw 电流命令峰值：2121
分析脚本给出最大过冲：80%
测试过程 yaw 范围：-11.46° ~ +0.54°
```

结论：

```text
yaw_k_vel_ff=50 明显偏大，速度前馈会造成过冲。
电流峰值不算高，还有调参空间。
20 dps 幅值偏小，GM6020 速度反馈以 6 dps 为粒度，低速测试分辨率有限。
```

### 2. yaw 速度阶跃，20 dps，`yaw_k_vel_ff=40`

输出目录：

```text
tools/out/tune/20260504_203337
```

命令要点：

```text
preset=speed-yaw
amp=20 dps
hold_ms=1200
duration=8 s
yaw_k_vel_ff=40
```

结果摘要：

```text
yaw 实测速度范围：-18 ~ +18 dps
yaw 电流命令峰值：1886
测试过程 yaw 范围：-2.93° ~ +2.38°
```

细看数据后发现：

```text
命令持续给 +20 dps 时，电流也持续输出，但实测速度很多采样点仍为 0。
第一段 +20 dps 实际只移动约 5.2°，理论上 1.2 s 应接近 24°。
```

结论：

```text
把 yaw_k_vel_ff 从 50 降到 40 后，明显压住了部分过冲。
但 20 dps 测试太小，容易被静摩擦、速度反馈粒度和结束姿态影响。
该结果不能作为最终参数，只能说明 50 偏大，40 比 50 温和。
```

### 3. yaw 速度阶跃，40 dps，`yaw_k_vel_ff=40`，长保持

输出目录：

```text
tools/out/tune/20260504_203722
```

命令要点：

```text
preset=speed-yaw
amp=40 dps
hold_ms=1200
duration=8 s
yaw_k_vel_ff=40
```

结果摘要：

```text
yaw 实测速度范围：-78 ~ +156 dps
yaw 电流命令峰值：6075
测试过程 yaw 范围：+1.69° ~ +50.25°
最后 yaw：约 +48.85°
```

结论：

```text
该实验理论单段位移约 48°，过大。
云台被推到接近正限位区域，后续数据被结束姿态和限位风险污染。
这次不能直接用于定参。
```

### 4. 回中保持

输出目录：

```text
tools/out/tune/20260504_203935
```

命令要点：

```text
preset=hold-pitch
yaw=0
pitch=10
planner=1
duration=5 s
```

结果摘要：

```text
起始 yaw：约 +48.87°
结束 yaw：约 +1.09°
结束 pitch：约 +9.98°
```

结论：

```text
planner 回中有效。
后续测试必须在实验后执行回中保持，否则下一轮起点会被污染。
```

### 5. yaw 速度阶跃，40 dps，`yaw_k_vel_ff=40`，短保持

输出目录：

```text
tools/out/tune/20260504_204132
```

命令要点：

```text
preset=speed-yaw
amp=40 dps
hold_ms=300
duration=5 s
yaw_k_vel_ff=40
```

结果摘要：

```text
测试起点：yaw≈+1.31°，pitch≈+9.24°
测试终点：yaw≈-43.63°，pitch≈+0.33°
测试过程 yaw 范围：-60.0° ~ +1.40°
yaw 实测速度范围：-120 ~ +66 dps
yaw 电流命令峰值：3932
```

结论：

```text
虽然 hold_ms 缩短到 300 ms，但 duration=5 s 导致 speed-yaw preset 跑了多轮。
正反向响应不对称使姿态逐轮漂移，最终碰到 yaw 负限位。
该实验也不能直接用于定参。
```

### 6. 回中保持

输出目录：

```text
tools/out/tune/20260504_204150
```

命令要点：

```text
preset=hold-pitch
yaw=0
pitch=10
planner=1
duration=4 s
```

结果摘要：

```text
起始 yaw：约 -35.86°
结束 yaw：约 -1.18°
结束 pitch：约 +9.34°
```

结论：

```text
回中后 yaw 已接近中心，但 pitch 还差约 0.66°。
后续正式测试前仍应检查末态，必要时延长回中保持时间。
```

### 7. yaw 速度阶跃，30 dps，`yaw_k_vel_ff=30`，短测试

输出目录：

```text
tools/out/tune/20260504_204713
```

命令要点：

```text
preset=speed-yaw
amp=30 dps
hold_ms=200
duration=1.6 s
yaw_k_vel_ff=30
yaw_hold_ff=0
```

结果摘要：

```text
测试起点：yaw≈-0.92°，pitch≈+8.47°
测试终点：yaw≈+4.11°，pitch≈+1.09°
测试过程 yaw 范围：-1.78° ~ +4.49°
yaw 实测速度范围：-36 ~ +30 dps
yaw 电流命令峰值：2352
```

分段观察：

```text
+30 dps 第一段：平均约 +16.8 dps，尾部约 +14.4 dps
-30 dps 第一段：平均约 -26.7 dps，尾部约 -27.6 dps
+30 dps 第二段：平均约 +21.0 dps，尾部约 +18.0 dps
```

结论：

```text
该测试没有碰限位，数据比前几次干净。
yaw_k_vel_ff=30 后过冲明显收敛，电流峰值也可接受。
但正向 +30 dps 明显跟不上，负向 -30 dps 更接近目标。
正反向不对称仍然明显。
```

### 8. 回中保持

输出目录：

```text
tools/out/tune/20260504_204728
```

结果摘要：

```text
起始 yaw：约 +0.63°
结束 yaw：约 +0.40°
结束 pitch：约 +9.33°
```

结论：

```text
回中后 yaw/pitch 满足下一轮短测试起点要求。
```

### 9. yaw 速度阶跃，30 dps，`yaw_k_vel_ff=30`，`yaw_hold_ff=500`

输出目录：

```text
tools/out/tune/20260504_204833
```

命令要点：

```text
preset=speed-yaw
amp=30 dps
hold_ms=200
duration=1.6 s
yaw_k_vel_ff=30
yaw_hold_ff=500
```

测试目的：

```text
验证是否存在固定方向负载/摩擦偏置。
此前 0 速保持时 yaw 经常需要约 500~600 正向电流，因此尝试用 yaw_hold_ff=500 做补偿。
```

结果摘要：

```text
测试起点：yaw≈+0.46°，pitch≈+8.68°
测试终点：yaw≈+6.08°，pitch≈+1.36°
测试过程 yaw 范围：+0.46° ~ +6.43°
yaw 实测速度范围：-36 ~ +36 dps
yaw 电流命令峰值：2755
```

分段观察：

```text
+30 dps 第一段：平均约 +19.5 dps，尾部约 +15.6 dps
-30 dps 第一段：平均约 -22.2 dps，尾部约 -20.4 dps
+30 dps 第二段：平均约 +17.1 dps，尾部约 +10.2 dps
```

结论：

```text
yaw_hold_ff=500 没有改善整体速度跟随。
它增加了电流峰值，并使负向响应变慢。
该参数不采用，已在测试后执行 tune set yaw_hold_ff=0 复位。
```

### 10. 回中保持

输出目录：

```text
tools/out/tune/20260504_204850
```

结果摘要：

```text
起始 yaw：约 +3.09°
结束 yaw：约 +1.76°
结束 pitch：约 +9.43°
```

结论：

```text
回中可用，但 yaw 仍略偏正。
后续测试前建议继续使用短实验，并在命令中显式设置 yaw_hold_ff=0。
```

### 11. yaw 速度阶跃，30 dps，`yaw_k_vel_ff=30`，`yaw_speed_kp=33`

输出目录：

```text
tools/out/tune/20260504_205033
```

命令要点：

```text
preset=speed-yaw
amp=30 dps
hold_ms=200
duration=1.6 s
yaw_k_vel_ff=30
yaw_hold_ff=0
yaw_speed_kp=33
yaw_speed_kd=0
```

测试目的：

```text
在不加 D 的前提下，小幅提高 yaw 速度环 P，观察是否改善正向跟随。
```

结果摘要：

```text
测试起点：yaw≈+1.56°，pitch≈+8.40°
测试终点：yaw≈+3.23°，pitch≈+1.35°
测试过程 yaw 范围：-2.34° ~ +4.50°
yaw 实测速度范围：-42 ~ +30 dps
yaw 电流命令峰值：2234
```

分段观察：

```text
+30 dps 第一段：平均约 +9.0 dps，尾部约 +6.0 dps
-30 dps 第一段：平均约 -28.5 dps，尾部约 -33.0 dps
+30 dps 第二段：平均约 +20.4 dps，尾部约 +16.8 dps
```

结论：

```text
yaw_speed_kp=33 没有稳定改善正向跟随。
负向响应更容易过冲到 -42 dps。
该参数不采用，已在测试后执行 tune set yaw_speed_kp=30 复位。
```

### 12. 回中保持

输出目录：

```text
tools/out/tune/20260504_205047
```

结果摘要：

```text
起始 yaw：约 -0.79°
结束 yaw：约 -0.75°
结束 pitch：约 +9.39°
```

结论：

```text
回中后位置满足下一轮短测试起点要求。
但 yaw_current_cmd 仍约 +1487，说明姿态保持时存在较明显静态电流需求，机械偏置/摩擦仍需关注。
```

## 当前总体结论

1. planner 已经可用于回中和大位移规划，回中效果可用。
2. 当前 `speed-yaw` preset 会周期性反复执行速度阶跃，长 duration 会让姿态持续漂移，不能直接作为最终速度环定参手段。
3. 速度阶跃必须控制理论位移，不能让 yaw/pitch 靠近限位。
4. `yaw_k_vel_ff=50` 偏大，已经观察到明显过冲。
5. `yaw_k_vel_ff=40` 比 50 温和，但在 40 dps 测试中仍出现明显过冲和正反向不对称。
6. 20 dps 测试幅值偏小，容易受到静摩擦和 6 dps 速度反馈粒度影响。
7. 40 dps 测试更有判别力，但必须缩短 hold 和 duration，避免撞限位。
8. 当前 yaw 正反向响应不对称明显，不能只靠控制参数硬补，需要同步检查机械因素。
9. 目前没有得到可以写入固件默认值的最终参数，只得到调参方向。
10. 新增短测试显示 `yaw_k_vel_ff=30` 比 40/50 更稳，但正向速度仍明显跟不上。
11. `yaw_hold_ff=500` 补偿方向不适合当前状态，已复位为 0，不采用。
12. `yaw_speed_kp=33` 不采用，已复位为 30。提高 P 没有稳定改善正向跟随，反而使负向更容易过冲。

## 当前调参方向

暂不加 D。

优先方向：

```text
当前 yaw_k_vel_ff 先保留 30 作为下一轮基线
显式保持 yaw_hold_ff=0
缩短速度阶跃 duration 和 hold_ms
每次测试前后回中
观察 yaw 正反向是否仍明显不对称
```

下一次建议不要继续提高 `yaw_speed_kp`。更值得排查的是机械/方向不对称，或在保持短测试的前提下试更细的前馈点，例如 `yaw_k_vel_ff=35`，同时显式保持 `yaw_hold_ff=0`、`yaw_speed_kp=30`。

```bash
rtk .\.venv\Scripts\python.exe tools\tune_rtt.py trial --preset speed-yaw --duration 1.6 --amp 30 --hold-ms 200 --sample-rate-ms 10 --set yaw_k_vel_ff=35 --set yaw_hold_ff=0 --set yaw_speed_kp=30
```

预期：

```text
单段理论位移约 6°
不应接近 yaw ±60° 限位
若正向明显改善且负向不过冲太多，可在 30~35 之间找更细的 yaw_k_vel_ff。
若负向过冲明显增加，则退回 yaw_k_vel_ff=30。
若正反向仍严重不对称，优先检查机械阻力、线束拖拽、电机方向和限位附近摩擦。
```

## 后续记录要求

每次实验结束后必须补充：

```text
输出目录
命令参数
起始 yaw/pitch
结束 yaw/pitch
是否接近限位
速度目标范围
实测速度范围
电流命令峰值
是否正反向不对称
下一次只改哪个参数
```
