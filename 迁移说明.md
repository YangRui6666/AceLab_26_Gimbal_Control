# AceLab_26_Gimbal_Control 仓库迁移说明

## 1. 背景与目标

本次迁移的目标是调整 GitHub 仓库 `AceLab_26_Gimbal_Control` 的主线来源，使其默认展示 `D:\ACE\Gv3\Gimbalv3` 的最新工程内容，同时保留旧仓库 `D:\ACE\AceLab_26_Gimbal_Control` 的历史提交记录，保证后续开发使用新的工程基线，历史追溯仍然完整可用。

本次迁移不涉及业务逻辑重构，不对工程目录做额外拼接或双目录并存处理，重点是完成 Git 历史组织方式的切换。

## 2. 迁移前状态

迁移实施前，两个本地仓库状态如下：

- `D:\ACE\Gv3\Gimbalv3`
  - 当前迁移基线分支为 `human`
  - 工作区干净，可以作为迁移起点

- `D:\ACE\AceLab_26_Gimbal_Control`
  - 当前历史来源分支为 `dev`
  - `dev` 分支相对远端 `origin/dev` 超前 1 个本地提交
  - 为避免历史遗漏，迁移时采用本地 `dev` 作为旧历史来源

## 3. 迁移目标形态

迁移完成后的目标仓库形态如下：

- GitHub 仓库名与远端地址不变，仍为 `AceLab_26_Gimbal_Control`
- 远端 `main` 分支展示 `Gv3` 的当前代码树
- 旧仓库历史保留在提交图中，可继续追溯
- 当前代码树中不保留“旧仓库代码与新仓库代码并排双目录”的结构
- 历史上新增一个 merge commit，将两段原本无共同祖先的历史连接起来

## 4. 实施过程

本次迁移按以下步骤完成：

### 4.1 建立回退点

在 `Gv3` 仓库中先建立迁移前回退锚点，便于后续需要时快速恢复：

- 标签：`gv3-pre-migration-20260423`
- 备份分支：`backup/human-pre-migration-20260423`

### 4.2 接入旧仓库历史

将旧仓库 `D:\ACE\AceLab_26_Gimbal_Control` 作为临时 remote `oldrepo` 接入 `Gv3` 仓库，并抓取其分支历史。

旧历史来源采用的是：

- `oldrepo/dev`

这样可以确保旧仓库本地 `dev` 中比远端多出的那 1 个提交也一并纳入最终历史。

### 4.3 创建迁移分支

以 `Gv3` 当前分支 `human` 为基线，新建迁移工作分支：

- `gv3-migration`

### 4.4 执行历史拼接

在 `gv3-migration` 分支上执行一次无共同祖先历史合并，使用策略如下：

- 允许无共同历史：`--allow-unrelated-histories`
- 保留当前代码树策略：`-s ours`

生成的关键合并提交为：

- `2118f75` `合并旧仓库历史，保留 Gv3 代码树`

该操作的效果是：

- 当前文件树仍然保持 `Gv3` 的代码内容
- 旧仓库历史被连接到当前提交链中
- 不会把旧仓库文件直接覆盖到当前工程目录

## 5. 本地验证情况

迁移完成后，已执行以下本地验证：

### 5.1 Git 状态检查

- `gv3-migration` 分支工作区干净
- 无合并冲突残留
- 提交图已经显示为新旧两段历史通过 merge commit 连接

### 5.2 工程构建验证

按当前仓库约定执行了 Debug 构建检查：

```powershell
rtk cmake --preset Debug
rtk cmake --build --preset Debug
```

验证结果：

- `rtk cmake --preset Debug` 配置成功
- `rtk cmake --build --preset Debug` 执行成功
- 构建输出为 `ninja: no work to do.`，说明迁移未引入新的构建异常

## 6. 远端推送过程

为降低风险，本次迁移先推送检查分支，再更新主线。

### 6.1 推送检查分支

先将迁移结果推送到远端检查分支：

- `gv3-migration-check`

用于在 GitHub 上确认以下内容：

- `Code` 页面显示的是 `Gv3` 当前代码树
- `Commits` 和提交图中能够看到旧仓库历史

### 6.2 更新远端主线

在确认检查分支无误后，将同一迁移结果更新到远端主线：

- `main`

推送过程中曾出现一次 HTTPS TLS 握手波动，但后续复核确认主线更新已成功完成。

## 7. 最终结果

最终远端分支状态如下：

- `refs/heads/main` -> `2118f751ea0b2bdecb59894c91f68f01e5e80ab1`
- `refs/heads/gv3-migration-check` -> `2118f751ea0b2bdecb59894c91f68f01e5e80ab1`

这说明：

- GitHub `main` 已正式切换到迁移后的结果
- GitHub 当前主线展示的是 `Gv3` 工程代码树
- 旧仓库历史已经通过 merge commit 保留在当前仓库历史中

## 8. 本次迁移未做的事情

为控制范围，本次迁移没有进行以下操作：

- 没有额外修改业务代码逻辑
- 没有重构目录结构
- 没有把旧仓库代码复制成并排目录保留在当前文件树中
- 没有在迁移过程中引入新的功能性改动

## 9. 迁移后的仓库使用说明

迁移完成后，后续开发建议直接基于当前 GitHub 仓库主线进行，不再把旧仓库作为独立主开发仓库使用。

如需查看迁移前 `Gv3` 的原始状态，可使用以下回退锚点：

- 标签：`gv3-pre-migration-20260423`
- 分支：`backup/human-pre-migration-20260423`

如需追溯旧仓库历史，可通过以下方式查看：

```powershell
git log --graph --oneline --decorate --all
```

或直接在 GitHub 的提交历史与网络图中查看 merge commit 之前的旧历史链。

## 10. 总结

本次迁移已经完成以下核心目标：

- 保持 GitHub 仓库名称和地址不变
- 将 `Gv3` 切换为远端主线展示内容
- 保留旧仓库完整历史，包括旧仓库本地 `dev` 超前远端的额外提交
- 通过一次安全的无共同历史合并完成历史接入
- 在迁移后完成本地构建验证与远端分支确认

当前仓库已经处于可继续开发的最终状态。
