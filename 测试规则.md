# 云台测试规则

## 基本原则

1. 每次只跑一个实验命令，避免多个测试互相污染结果。
2. 所有测试命令使用 Python 虚拟环境执行。
3. 每次实验前先让云台回到接近中心的稳定姿态。
4. 每次实验后必须回中并保持，确认下一轮起点干净。
5. 单次实验的理论位移要远离 yaw/pitch 限位。
6. 调参过程中永远不加 D 项，`*_kd` 保持为 0，除非后续明确推翻这条规则。

## 推荐起止姿态

测试前后建议使用：

```text
yaw = 0 deg
pitch = 10 deg
planner = 1
```

回中命令示例：

```bash
rtk .\.venv\Scripts\python.exe tools\tune_rtt.py run --preset hold-pitch --duration 4 --sample-rate-ms 20
```

回中后需要检查末态，目标是大致满足：

```text
abs(yaw) < 2 deg
abs(pitch - 10 deg) < 1 deg
速度接近 0
未靠近机械限位
```

## 速度阶跃测试

速度阶跃用于调速度环和速度前馈，不能让云台在一次实验里持续漂移到限位。

当前不建议使用长时间、多周期的 `speed-yaw` 默认测试。`duration` 和 `hold_ms` 要一起控制，保证单段理论位移较小：

```text
理论单段位移 deg = speed_dps * hold_ms / 1000
```

建议单段理论位移先控制在 `5 deg ~ 12 deg`。例如：

```bash
rtk .\.venv\Scripts\python.exe tools\tune_rtt.py trial --preset speed-yaw --duration 1.6 --amp 30 --hold-ms 200 --sample-rate-ms 10 --set yaw_k_vel_ff=30
```

如果测试过程中 yaw 或 pitch 接近限位，当前实验数据不能直接用于定参，必须先回中，再重新设计更短或更小幅的实验。

## 阶跃与跟随测试顺序

建议按下面顺序推进：

1. 速度环测试：先调 `*_speed_kp`、`*_speed_ki`、`*_k_vel_ff`。
2. 角度 direct 阶跃：确认位置环响应、超调和稳态误差。
3. planner 阶跃：再调 `*_max_vel_dps` 和 `*_max_acc_dps2`。
4. 跟随测试：先低频小幅 sine，再逐步提高频率和幅度。

不要用 planner 掩盖速度环或位置环本身的问题。速度环没调顺前，planner 只能让测试看起来温和，不能证明实际跟随足够快准。

## 调参规则

### 不加 D

以下参数保持为 0：

```text
yaw_location_kd
yaw_speed_kd
pitch_location_kd
pitch_speed_kd
```

出现过冲或震荡时，优先调整：

```text
*_k_vel_ff
*_speed_kp
*_speed_ki
*_location_kp
*_max_vel_dps
*_max_acc_dps2
```

并同步检查机械因素：

```text
重心
摩擦
线束拖拽
限位
电机方向
安装松动
```

## 每次实验必须记录

每次测试结束后至少记录：

```text
输出目录
起始 yaw/pitch
结束 yaw/pitch
yaw/pitch 是否接近限位
速度目标范围
实测速度范围
电流命令峰值
是否出现明显正反向不对称
下一步只改哪个参数
```

如果结束姿态不干净，先回中，不继续调参。

## 数据分析脚本

重复的数据分析动作使用：

```bash
rtk .\.venv\Scripts\python.exe tools\analyze_tune_data.py
```

该脚本只读取 `tools/out/tune/<输出目录>/raw_samples.csv` 和 `session.json`，不连接 J-Link，不向云台发送命令。

分析最近 3 次输出：

```bash
rtk .\.venv\Scripts\python.exe tools\analyze_tune_data.py --latest 3
```

分析指定输出目录：

```bash
rtk .\.venv\Scripts\python.exe tools\analyze_tune_data.py 20260504_204713 20260504_204833 20260504_205033
```

把分析结果写成 Markdown 报告：

```bash
rtk .\.venv\Scripts\python.exe tools\analyze_tune_data.py 20260504_204713 20260504_204833 20260504_205033 --out tools\out\tune\analysis_latest.md
```

脚本会自动汇总：

```text
起始 yaw/pitch
结束 yaw/pitch
yaw/pitch 范围
速度目标范围
实测速度范围
电流命令峰值
单段理论位移
各速度阶跃分段的平均速度、尾部速度、过冲、稳态误差
正反向尾速差异
```

脚本输出的 `flags` 含义：

```text
OK        当前摘要未发现明显数据污染
LIMIT     测试接近 yaw/pitch 限位，不能直接用于定参
DIRTY_END 结束姿态不在推荐回中范围，下一轮测试前必须回中
ASYM      正反向响应不对称明显，优先检查机械阻力/线束拖拽/电机方向
BIG_STEP  单段理论位移过大，应缩短 hold_ms 或降低 amp
```

出现 `LIMIT` 或 `BIG_STEP` 时，本次实验只用于判断测试设计问题，不用于更新控制参数。

## 当前经验结论

已有测试显示：

```text
yaw_k_vel_ff = 50 时，yaw 速度阶跃过冲明显。
yaw_k_vel_ff = 40 时，小幅测试过冲下降，但长时间 speed-yaw 仍会因姿态漂移污染结果。
40 dps / 1200 ms 的速度阶跃理论位移约 48 deg，过大，不适合作为常规速度环测试。
后续 speed-yaw 测试应缩短 hold_ms 和 duration，先避免碰限位，再判断参数方向。
```
